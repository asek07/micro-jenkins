#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('Example Build') {
            steps {
                echo "This is working!"
                sh "mvn --version"
            }
        }
    }
}